jupyterlab==3.6.3
matplotlib==3.7.1
# MediaPipe requires numpy < 2. Keep numpy in the 1.x series so mediapipe binaries can be installed.
numpy>=1.24.3,<2
seaborn==0.11.2
scikit-learn==1.2.2
statsmodels==0.14.0
pytest==7.4.0
testbook==0.4.2
parsenvy==3.0.2
black==23.3.0
# protobuf kept in 4.x range for this environment (some older packages expect <5)
protobuf>=4.23.3,<5
nbdime==4.0.2
mediapipe==0.10.21
pydantic
openpyxl
reportlab

# NOTE: streamlit, pandas>=2.x, plotly and streamlit extensions were intentionally removed
# from this file because they require numpy>=2. Install them into a separate environment
# using `requirements_streamlit.txt` (created alongside this file).